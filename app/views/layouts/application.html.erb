<!DOCTYPE html>
<html>
  <head>
    <title>AppOlivia</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav>
  <% if logged_in? %>
      <a href="/logout">Log Out</a>
        <% else %>
        <a href="/registration/new">Create account</a></h2>
        <a href="/session/new">Login</a></h2>
  <% end %>
  <h1 class="text-center bg-red-200">Olivia</h1>
  </nav>

    <%= yield %>
  </body>
</html>
